use super::AResult;
use crate::ast::*;

/* Semantically checks an entire program. */
/* This function initialises the symbol table and function table. */
fn program(_program: &Program) -> AResult<()> {
  todo!();
}
