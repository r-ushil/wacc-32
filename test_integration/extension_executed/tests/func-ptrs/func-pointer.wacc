begin
    int bar(int x) is 
        int y = x + 1;
        return y
    end

    int foo(int(int) b, int w) is
        println w;
        int z = call b(w);
        println z;
        return z
    end

    int res = call foo(bar, 3);
    println res;
    exit res
end
