begin
    int succ(int x) is
        return x + 1
    end

    int applyThree(int(int) b) is
        int z = call b(3);
        return z
    end

    int res = call applyThree(succ);
    
    exit res
end
