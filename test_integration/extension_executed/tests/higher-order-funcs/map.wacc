import import-files/hof.wacc
begin
    int[] orig = [1, 2, 3];
    int(int) succ = int(int x) is return x + 1 end;

    int[] res = call map(succ, orig);

    int total = 0;
    for i : res do
        total = total + i
    done ;

    exit(total) #total = 9
end