begin
    int bar(int x) is 
        int y = x + 1;
        return y
    end

    int foo(int(int) b, int w) is
        int z = call b(w);
        return z
    end

    int res = call foo(bar, 3);
    exit res
end
