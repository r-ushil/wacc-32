begin
    int(int) foo = int(int x) is 

        int(int) bar = int(int y) is 
            return y + 2 
        end;
        
        int res = call bar(x);
        return res
    end;

    int outer_res = foo(1);
    exit outer_res
end