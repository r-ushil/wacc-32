.data:
.text:
.global main:
main:
Program { funcs: [], statement: Sequence(Declaration(Int, "x", Expr(IntLiter(0))), Sequence(Print(StrLiter("We can print the hash character: ")), Sequence(Println(CharLiter('#')), Println(StrLiter("We can also print # when its in a string."))))) }.generate(_, 4):
